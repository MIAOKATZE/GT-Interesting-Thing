# GT-Interesting-Thing 第二轮 BUG 处置汇总（2026-08-22 R2）

- 分支：`fix/20260822-r2` | 施工基线：`18d2275` | 施工者：it-editor（r2-20260822）
- 输入：深查报告 it-survey-a（B2-01/02/04/05/08/09/11 + B2-15 复验）、it-survey-b（B2-03/06/07/10/12/13②/14 + B2-15 复验）
- 批次执行序（严格遵守拓扑）：① B2-01+B2-05 → ③ B2-03+B2-12 → ④ B2-10/13/14/13② → ⑤ B2-06/07 → ② B2-02+04+08+09+11（热点文件单执行者串行）
- 总结果：**15/15 全部处置：修复 14 条（13 个 fix commit + 2 个 docs commit 内含 2 条）+ 已闭合复验 1 条（B2-15）；登记不实施 0 条；无 revert；每条目独立 `./gradlew build -x test` 全绿后提交**

---

## 一、逐条处置明细

### [IT-B2-01] 抽奖请求包坐标零校验【高】 — 已修复
- 深查结论：LotteryRequestPacket.findMachine 四字段零校验，三条攻击链闭合（扣他人输入槽/溢出奖品进请求者背包/奖品塞他人出货槽）。
- 处置：`LotteryRequestPacket` 新增带 player 的 `findMachine`——同维度/8 格距离/GUI 会话绑定三重校验（对齐 MailActionPacket IT-BUG-03 范式，instanceof pattern 仓内既有）；校验失败 `sendFailure` 整包拒绝，封死"无机器直接给玩家"退化路径；`LotteryManager.findMachine` 静态版保留给 `dispatchAll` 延迟出货（出货时玩家可能已关 GUI，坐标快照已可信）。
- Commit：`53bd331`
- 验证：build 全绿；`git show 53bd331`（1 file, +77/-4，仅 LotteryRequestPacket.java）。

### [IT-B2-02] C2S 在 Netty 线程直跑交易链竞态【高】 — 已修复
- 深查结论：MUI2 2.3.70 实物解包证实 C2S 同步值回调在 Netty IO 线程直跑（sources 树 grep addScheduledTask 零命中）；两玩家同机并发购买后写覆盖先写；nekoImportMeCoin 等 HashMap Netty 写×主线程读并发面。
- 处置：`NekoVMGuiV2` 挂静态 `ConcurrentLinkedQueue<Runnable> SERVER_ACTIONS` + `scheduleServerAction/drainServerActions`（同 MailHandler/LotteryHandler 范式，单任务异常仅记日志）；`MTENekoVendingMachineV2.onPostTick` 服务端分支逐 tick 消费。5 组 C2S setter 服务端动作主体整体投递：tradeRequestSync / nekoImportMeCoin（每币种）/ nekoEjectCoin / nekoEjectCoinStack / ME 取回按钮——含 HashMap put 归一主线程；客户端侧 changeListener 保持原语义直跑（无服务端动作），闭包内各动作方法自带 baseMetaTileEntity/multiblock 存活守卫。
- Commit：`cb5e654`（与 B2-04 联动）
- 验证：build 全绿；`git show cb5e654`（2 files：NekoVMGuiV2.java + MTENekoVendingMachineV2.java）。

### [IT-B2-03] 首登礼包去重标志游离 NBT 副本【中】 — 已修复
- 深查结论：getCompoundTag 缺键返回不挂接实例 → setBoolean 落空 → 精简环境每次登录重发；仓内自证 GTITGiftCommand:578 正确写法在侧。
- 处置：`PlayerLoginHandler` setBoolean 后补 `playerData.setTag(EntityPlayer.PERSISTED_NBT_TAG, persisted)` 无条件回写（2 行级，照仓内既有形式）。
- Commit：`b5cc65e`
- 验证：build 全绿；`git show b5cc65e`（1 file, +4）。

### [IT-B2-04] meTransferQueue 非线程安全 ArrayList【中】 — 已修复（随 B2-02 天然闭合）
- 深查结论：remove(0)（Netty）× iterator+remove（主线程）并发 CME/槽错位；深查补充发现：登记的独立修法 CopyOnWriteArrayList 与 `iterator.remove()` 不兼容（COWAL 迭代器不支持 remove）。
- 处置：随 B2-02 投递后全队列仅主线程访问。验证 grep 全部触点：`:521/:524 retrieve`（已投递主线程）、`:539/:556-559`（sync getter，主线程 detectAndSendChanges）、`:864 add`（onPostTick 链）、`:990/:1030`（NBT 读写）、`:1169-1215 processMeTransferQueue`（onPostTick）——无 Netty 残留路径。
- Commit：`cb5e654`（与 B2-02 同 commit）
- 验证：见 B2-02；触点清单见本行 grep 记录。

### [IT-B2-05] mail/lottery 回包无速率限制（网络放大）【中】 — 已修复
- 深查结论：单请求 ~50B → 回包全池×10 条目 NBT 或 50 封邮件全文，放大数百倍。
- 处置双层：① 幂等无变更不回发——mail `processAction` 按 markRead 返回值/granted>0/result==0 追踪 `changed`（COMPOSE 仍全量），lottery `sendFailure` 去掉 sendSyncToClient；② `MailNetworkManager`/`LotteryNetworkManager` 的 `sendSyncToClient` 入口加 per-player 250ms 冷却（`ConcurrentHashMap<UUID,Long>`，冷却窗从上次实际放行起算，成功操作首调不拦；登录首推/全服广播在窗外不受影响）。
- Commit：`4304392`
- 验证：build 全绿；`git show 4304392`（4 files, +73/-5）。

### [IT-B2-06] 团队钱包切换无余额合并 + 个人 .dat 残留【低】 — 已修复（文档型最小修）
- 深查结论：getWallet 团队优先路由无迁移、saveWallet 团队分支提前 return、全文件零 File.delete——行为自洽但语义未文档化；清理 .dat/余额合并属行为变更不建议本轮做。
- 处置：`NekoWalletManager.getWallet/saveWallet` javadoc 明示"入队冻结个人余额、退队回入队前快照；个人 .dat 保留作退队恢复点"（与 O2-18(a) 同一决策，单点落位）。
- Commit：`23f9450`（docs）
- 验证：build 全绿；文档审读。

### [IT-B2-07] NekoWallet:18 注释描述已删行为【低】 — 已修复
- 深查结论：注释"团队钱包每次 getWallet 时被覆盖为最近查询者"失实（团队分支仅 setTeamId，setPlayerId 全文件仅个人分支 1 处）。
- 处置：注释勘误为"团队钱包不使用本字段，归属由 teamId 表达；playerId 仅个人钱包设置"。
- Commit：`537217b`（docs）
- 验证：build 全绿；`git show 537217b`（1 行注释）。

### [IT-B2-08] 拆机时 outputBuffer 队列物品丢失【低】 — 已修复
- 深查结论：onRemoval 服务端分支零处理，下落动画窗口内拆机已付款产物随 MTE 消失。
- 处置：`onRemoval` 服务端分支 poll 循环逐件 `dropItemsNearMachine`（同类私有方法直调，GT 拆机时序内 baseTE 坐标仍有效）；单件异常仅记日志不中断；区块卸载路径仍走 NBT 无损。
- Commit：`a8f43f5`
- 验证：build 全绿；`git show a8f43f5`。

### [IT-B2-09] 抽奖出货绕过 ME 队列 18 条上限【低】 — 已修复
- 深查结论：outputIntoSlot :863-865 meOutputMode 时无条件 add，交易路径两处预检不覆盖抽奖链。
- 处置：入队点统一守门 `meTransferQueue.size() < MAX_ME_QUEUE_SIZE`——超限条目物品已在出货槽（先落槽），等价"按本地槽保留"，口径对齐交易路径。
- Commit：`3a167a1`
- 验证：build 全绿；`git show 3a167a1`。

### [IT-B2-10] BGM 淡出强停倒计时残留误杀新 BGM【低】 — 已修复
- 深查结论：updateFade 自然结束清理分支不重置 forceStopDeadline（对照 stopSound:479 有重置），旧 deadline 到期把 3 秒内新 BGM 强停。
- 处置：自然结束清理分支补 `forceStopDeadline = 0L`；防御性在 handleSoundCreated 捕获新声音时同样清零（全文件触及点由 4 处变 6 处，语义完整覆盖）。
- Commit：`a896f2b`
- 验证：build 全绿；`git show a896f2b`。

### [IT-B2-11] System.out.println 治理（33 处）【低】 — 已修复
- 深查结论：NekoVMGuiV2 28 处（[NekoInput]/[NekoParticle]/[NekoImportCoins]/[NekoForceSync] 四段）+ NekoFallingItemSlotFactory 4 处 + NekoItemSlotWithDepth 1 处。
- 处置：全量转 `GTInterestingThing.LOG.debug`（消息体原样保留排查价值，受日志级别控制）；两客户端类补 import；转换后行宽超限由构建管线内置 `spotlessApply` 重排（非新依赖）。
- Commit：`1784bfd`
- 验证：build 全绿；`grep -rn "System.out.println|System.err.println" src/main/java` = **0 处**。

### [IT-B2-12] admin 加载的离线签到数据误计在线时长【低】 — 已修复
- 深查结论：tickOnlineMinute 对 signInDataMap 全量条目 addOnlineSeconds(60)，仅推送按在线门控（累计与推送门控分离即缺陷本体）。
- 处置（深查推荐 A）：循环内先 `getPlayerByUUID` 判在线，离线 continue（不累计不推送）；javadoc 同步勘误。
- Commit：`e090039`
- 验证：build 全绿；`git show e090039`。

### [IT-B2-13] CommonProxy.preInit 三处裸调【低】 — 已修复
- 深查结论：GiftConfig.init(:85)/MuteConfig.init(:114)/PlayerLoginHandler 注册(:117-119) 三处裸调，吞异常安全性已核（默认值不 NPE，注册失败降级可接受）。
- 处置：三处包 try/catch (Throwable) + `GTInterestingThing.LOG.error("[0/3] ...")`，对齐邻侧 DailySignInConfig 既有写法。
- Commit：`85ee4e3`
- 验证：build 全绿（行为等价性由编译+启动冒烟覆盖，深查结论：防御性无运行时验证锚点）。

### [IT-B2-13②→O2-13②] mcmod.info 版本 token 注入【构建面】 — 已修复
- 深查结论：GTNH convention 插件自带 mcmod.info token 展开（MUI2 同族 4 行构建实物证据），登记册"自写 processResources"修法过重，免动 gradle 文件。
- 处置：`src/main/resources/mcmod.info` 1-4 行改 `${modId}/${modName}/${modVersion}/${minecraftVersion}`（与 MUI2 逐字同形）；未动任何 gradle 文件。
- Commit：`236c419`
- 验证（构建产物 grep，实测）：`build/resources/main/mcmod.info` → `"version": "v1.7.41-fix-20260822-r2.14+1784bfd857"`；最终 jar `build/libs/gtit-v1.7.41-fix-20260822-r2.14+1784bfd857.jar` 内 mcmod.info 同值（干净树无 -dirty，sha 与 HEAD 一致）；对照施工前 jar 均为字面量 "1.7.41"。版本串含 branch+sha 属 convention 插件标准语义，RELEASE_VERSION bump 即自动流入。
- 发布核对项（移交）：发版清单加"构建产物 mcmod.info 版本=预期版本"检查。

### [IT-B2-14] 纪念日祝福防重键用列表序号【低】 — 已修复
- 深查结论：anniversaryIndex 随遍历递增，增删纪念日使序号漂移 → 剩余条目同年重发；AnniversaryEntry 无稳定 id。
- 处置（深查推荐 A，零存档格式变化）：防重键改 `"anniversary_" + entry.getName() + "@" + entry.getMonthDay() + "_" + year`（getName 空安全由构造器保证）；同名同日两条合并为一年一封为最小语义取舍；顺带移除死计数器。
- Commit：`80e2696`
- 验证：build 全绿；`git show 80e2696`。

### [IT-B2-15] mcmod.info 版本漂移【低·复发】 — 已闭合复验通过，不复修（符合调度）
- 复验（18d2275 基线）：`mcmod.info:7` = `"1.7.41"` = `gradle.properties:7 RELEASE_VERSION`——上轮应急字面量闭合在位 ✓。
- 本轮增益：B2-13②（`236c419`）token 化后复发机制（只改 gradle.properties 的 bump 再漂移）已根治，无需额外动作。

---

## 二、新发现登记（本轮不实施）

1. **NF-R2-01：NekoVMGuiV2 另有 6 组未入册 C2S setter 同型 Netty 直跑**。施工 B2-02 时实测：`ejectAllCoinsSync`(:937)/`importCoinsSync`(:947)/`ejectItemsSync`(:957)/`fillPlayerInventorySync`(:967)/`meOutputModeSync`(:978)/`favouriteToggleSync`(:791) 六组 setter 的服务端动作（doNekoEjectAllCoins/doNekoImportCoins/doNekoEjectItems/doNekoFillPlayerInventory/setMeOutputMode/toggleFavourite）同样仅有 `isClient()` 守卫、无线程切换，在 Netty IO 线程直跑并涉及机器槽 setStackInSlot/钱包/dispenseItemStacks 世界实体写——与 B2-02 同型竞态面。深查报告仅授权 5 组，按"范围膨胀登记不实施"纪律未顺手修。建议下轮统一收口：可直接复用本轮已落地的 `scheduleServerAction` 设施，每组约 5 行。
2. **B2-04 独立修法不可行性**（深查补充发现，随档移交）：CopyOnWriteArrayList 迭代器不支持 remove，若未来需独立修须改 poll 循环或以 `synchronized(meTransferQueue)` 包 :520-530 与 :1168-1223 两路径。

## 三、阻塞与偏差记录

- 无构建阻断、无 revert、无降级登记。
- 执行偏差 1 处（已自纠）：B2-08/B2-09 同文件（MTENekoVendingMachineV2.java），首建后为保每条目独立 sha，临时回退 B2-09 hunk 先提交 B2-08（`a8f43f5`）再重放提交 B2-09（`3a167a1`），两次均独立 build 全绿。
- B2-11 施工中 spotlessJavaCheck 一次失败（LOG.debug 调用签名变长导致行宽重排差异），以构建管线内置 `gradlew spotlessApply` 修复后 build 全绿——非代码逻辑问题、非新依赖。

## 四、收尾状态

- 最终工作区 clean；`18d2275..HEAD` 共 15 个 commit（13 fix + 2 docs，含本汇总 commit 前的 14 个 + 本文件）。
- 最终 build 全绿记录：`./gradlew build -x test` → BUILD SUCCESSFUL（每条目独立执行，共 11 次）。
- 完整报告已 post：Blackboard `runs/r2-20260822/results/it-editor`。

---

## 五、OPT 批①③逐条处置明细（it-opt-editor 波，2026-08-22）

- 输入：深查报告 B（Blackboard `runs/r2-20260822/results/it-survey-b`，OPT 33 锚点快核＋3 处微漂移）＋登记文件《全局调查-优化建议-第二轮.md》（批内条目按登记描述逐条深查后实施）。
- 施工基线：`62cc71e`（BUG 15 条收束后）；本波不碰调度器/GUI 大件（批②④⑤留后续波）。
- 总结果：**批①③ 9 条全部处置——实施 8 条（8 个独立 commit）＋单点闭合复验 1 条（O2-18a）；降级登记 1 处（O2-04 的 +2 点误判）；每条目独立 `./gradlew build` 全绿后提交。**

### [IT-O2-01] 成就系统骨架配置开关默认关闭【P1·护栏】 — 已实施
- 深查结论：锚点漂移至 CommonProxy `:350-355`（init 成就网络包）/`:445-452`（postInit BQ 桥接）/`:503-511`（serverStarted 成就管理器）——BUG 波 B2-13 防御包裹后行号 +16，三处调用与"已初始化"误导日志原样；achievement 包仅 CommonProxy 一个外部引用者，Trigger 钩子全项目零调用，关闭无副作用面。
- 处置：Config 新增布尔项 `enableAchievements`（默认 false，Configuration.getBoolean 落 config/gtit.cfg）；三处调用以开关包裹，关闭时改打"[x/3] 成就网络包已跳过（enableAchievements=false，未启用）"级日志；不动 852 行 TODO 主体，为 LongPlan M4 决策留护栏。
- Commit：`aee51b5`（chore）
- 验证：build 全绿；`git show aee51b5`（2 files，Config.java +11 / CommonProxy.java +40-14）。

### [IT-O2-02] RegistrationManager 三层自反转发删除【死代码】 — 已实施
- 深查结论：全项目引用仅 MachineLoader 1 处；"创建 registrar→addRegistrar→立即 registerAll"自反调用确认（实际仅 1 个注册任务注册 1 台 MTE 14610）；异常隔离在外层 Runnable（CommonProxy sAfterGTPreload 队列）已重复存在。
- 处置：initMachines 直调 `new MultiblockMachineRegistrar().registerAll()`，保留 try-catch Throwable 异常隔离（对齐原 registerAll 语义）；删除 RegistrationManager.java（57 行）；执行时机与注册 ID 不变。
- Commit：`2d81779`（refactor；首提交漏 stage MachineLoader，amend 补齐，见 §七偏差）
- 验证：build 全绿；`git show 2d81779`（2 files，+11/-67）。

### [IT-O2-14] ConfigMigrationUtil 扩展面收尾【一致性】 — 已实施
- 深查结论：LotteryConfig/BlessingConfig 两处 migrateFromLegacy 尾段各 6 行手写".bak 退役 + 双日志"与 retireLegacyAsBak 逐行同构（REPLACE_EXISTING 语义一致），sweep 只收编了 4 处遗漏此 2 处。
- 处置：两处尾段各替换为 1 行 `retireLegacyAsBak(legacyPath, newPath, 配置名, 文件名)` 调用（日志文案逐字保留），顺带清理各自孤儿 StandardCopyOption import、补 ConfigMigrationUtil import；净减 12 行。
- Commit：`c908d5e`（refactor）
- 验证：build 全绿；`git show c908d5e`（2 files，+4/-16）。

### [IT-O2-16] 4 个孤儿纹理资源删除【死代码】 — 已实施
- 深查结论：当前树复扫 gui/nei.png、gui/nei_header.png 全仓 code/json/lang 零引用；covers/ 目录仅剩 wireless_connector_input/output.png.mcmeta 两孤儿动画元数据（同名 png 不存在）。
- 处置：删 4 文件；nei 两张设计底稿移入本地 `plan/assest/` 素材库（plan/ 目录 git-ignore，不入运行时资源树与版本库），关闭第一轮四.8 遗留。
- Commit：`8a48ba0`（chore）
- 验证：build 全绿（资源删除后打包通过）；`git show 8a48ba0`（4 deletions）。

### [IT-O2-18a] 团队钱包 .dat 残留决策 javadoc【文档型】 — 单点闭合复验，不再动
- 复验：方案 (a)（保留 .dat 作退队恢复点）已由 BUG 波 B2-06 commit `23f9450` 落地——NekoWalletManager `:79-80`（getWallet）与 `:134`（saveWallet）javadoc 明示"入队冻结个人余额/退队回快照/.dat 保留作恢复点"，commit message 自注"与 O2-18(a) 同一决策单点落位"。本轮无新增动作。

### [IT-O2-09] 戒指模板三步收敛·第一步【死代码】 — 已实施（第一步）
- 深查结论：RingIronheart:32-35 / RingWindrider:31-34 两处 getBaubleType 重写与 BaseRing:35-37 逐字相同（均返回 BaubleType.RING），死重写确认。
- 处置：删除两处重写及各自孤儿 BaubleType import，原位留一行 O2-09 标记注释；第二步 BaseRing helper、第三步 PotionRingBase 按登记留后续批次。
- Commit：`abff2a3`（refactor）
- 验证：build 全绿；`git show abff2a3`（2 files，+2/-10）。

### [IT-O2-04] TeamDataProvider 门面统一 Teams 探测/降级/日志【架构·批③】 — 已实施（11 点）
- 深查结论：11 个登记降级点在当前 HEAD 全部命中（行号漂移：NekoVMGuiV2→:1265、NekoWalletManager→:121/:151/:281、CommonProxy 注册→:319）；**登记册外 +2 点证伪**——survey-b 称 CommonProxy 另有 :235/:284 两处"同 Teams 注册簇"NCDFE，实测（含回溯 18d2275 基线）均为 Baubles-Expanded 槽位扩展 API 的检测（expandBaublesRingSlots/ensureBaublesRingSlots），非 Teams 簇，**降级登记不收编**（见 §六）；NekoWallet:18 注释已由 B2-07 `537217b` 闭合、notifyWalletChanged 成员遍历已由 sweep 脏标记链吸收，两项搭车点无需再动。
- 处置：新建 `trade/TeamDataProvider`（~170 行）：`isAvailable()` 一次探测（Class.forName 只加载不初始化，volatile 缓存）+ `getTeam/getTeamById/getData/resolveTeamKey/forEachOnlineMember/markTeamDirty/registerTeamData` 统一 API；替换 7 业务文件 11 点——NekoWalletManager 3（getTeamWallet/saveWallet 团队分支/flushTeamBalance）+NekoHistoryManager 3（findTeam/getTeamData/markTeamDirty 委托化）+LotteryManager 1（resolveTeamKey）+NekoNotificationScheduler 1（notifyTeam 广播）+NekoTradeExecutor 1（getMaxTradesInCooldown）+CommonProxy 注册 1；10 个回退值逐一保留（钱包→个人/保底键→playerId/限额→1/模式→PERSONAL/历史→个人文件）；客户端路径 NekoVMGuiV2#getWalletMode 按登记维持异常驱动不动；日志统一为"探测 warn 一次 + 各操作 error"。
- Commit：`be72fe8`（refactor；净减 75 行样板）
- 验证：build 全绿（spotlessApply 一次格式修复后通过）；`git show be72fe8`（7 files，+46/-121）。

### [IT-O2-17] 显式 saveWallet 收敛为脏标记口径【一致性·批③】 — 已实施
- 深查结论：10 处显式调用当前树全命中（NekoVMGuiV2 6: :5379/:5632/:5688/:5749/:5897/:5954＋LotteryManager 2＋DailySignInManager 1＋NekoTradeExecutor 1）；逐处核实前置——每处余额变动均经 addCount/tryDeduct/resetCount，而 NekoWallet 四个变动方法全部自动触发 notifyChanged→notifyWalletChanged→脏标记登记（个人钱包 dirtyPersonalWallets + 待推送键），三重兜底（NekoWalletHandler 周期 5 分钟/登出 unloadWallet/停服 saveAll）在位；团队钱包 markDirty 由 ~100ms 冲刷链（flushTeamBalance）托管，覆盖 GTNHLib onWorldSave 仅落 DIRTY 团队约束（原 v1.7.6 G6⑤ 注释的担忧由冲刷链时序保证）。
- 处置：撤除 10 处显式 saveWallet（含 LotteryManager walletDirty 品种旗标整块、NekoTradeExecutor G6⑤ 注释改写为统一口径说明）；saveDirtyWallets/NekoWalletHandler 两处注释同步改写"兜底覆盖未走显式 saveWallet 的路径"→"脏标记即唯一落盘触发口径"；保留登出/停服两个生命周期显式钩子。
- Commit：`e81ad7d`（refactor）
- 验证：build 全绿；`git show e81ad7d`（6 files，+23/-30）；全仓 grep `.saveWallet(` 外部调用归零。

### [IT-O2-21] 冲刷链 removeAll 双扫微观察【性能·批③】 — 已实施（搭车 O2-04 后单 commit）
- 深查结论：flushBalanceNotifications 取批 `new ArrayList<>(set)` + `removeAll(keys)` 两趟扫描（removeAll 对 List 参数 O(n·m)）确认；键量级极小（≤在线数×团队数），登记定性为"顺手改"。
- 处置：改弱一致迭代器单趟直取（边取边 remove），语义等价——冲刷期间新到的键仍留下一批；作为独立 perf commit 收口本条"已从优化视角复核"的审计结论。
- Commit：`921c3ed`（perf）
- 验证：build 全绿；`git show 921c3ed`（1 file，+9/-3）。

## 六、降级登记与待后续波

- **降级 1 处（前提不成立不实施）**：survey-b 新发现的 CommonProxy `:235/:284`（今 :251/:300）两处 NCDFE catch 经 18d2275 基线回溯复核，属 Baubles-Expanded 槽位扩展 API 探测（baubles.api.expanded.BaubleExpandedSlots），非 GTNHLib Teams 注册簇——不并入 O2-04 门面（错误收编会混淆两个 mod 的探测语义）。若未来做 Baubles-Expanded 探测统一，另立条目。
- **O2-13②**：BUG 波 `236c419` 已 token 化闭合（连带根治 B2-15），本波按调度不再动。
- **批②（横切设施）已实施完毕**（it-opt-editor2 波，见第九～十一节）：O2-03 调度器 / O2-12 PlayerLookup / O2-06 一期（PlayerResolver）/ O2-B01 / O2-B02 / O2-B03。
- **批④（编辑域）已实施完毕**（it-opt-editor3 波，见第十二～十四节）：O2-05 策略表 E1-E4（A03 施工图，4 commit 逐 build 全绿）。
- **批⑤（GUI/机器大件）前置已就绪待后续波**（批④编辑域收束，依赖前置满足）：O2-B04 + O2-08 G0-G6 + O2-07 M1/M2（含 O2-19）；O2-15 折入 G0；A01/A02 蓝图、A04 归 C1-C3。
- **批⑥（缓办触发制）按触发条件登记不动**：O2-10 / O2-11 / O2-20 / O2-B05 / O2-B06 / O2-B07。

## 七、OPT 波阻塞与偏差记录

- 无构建阻断、无 revert。
- 执行偏差 1 处（已自纠）：O2-02 首提交只 stage 了 `git rm` 的 RegistrationManager 删除、漏 stage 改写的 MachineLoader，发现后立即 `git commit --amend` 补齐（期间无后续提交，历史线性，最终 `2d81779` 两文件完整）。
- O2-04 施工中 spotlessJavaCheck 一次失败（TeamDataProvider Class.forName 调用换行风格），以构建管线内置 `gradlew spotlessApply` 修复后 build 全绿——与 BUG 波 B2-11 同型，非代码逻辑问题、非新依赖。

## 八、OPT 波收尾状态

- 工作区 clean；`62cc71e..HEAD` 共 8 个实现 commit（chore 2 / refactor 5 / perf 1）+ 本汇总 docs commit。
- 每条目独立 `./gradlew build` 全绿（波内共 9 次，含 O2-21 收尾复跑）；jabel 管线全程在位，无新依赖、未触碰 libs/ 与 VendingMachine、无 git Tag。
- 报告已 post：Blackboard `runs/r2-20260822/results/it-opt-editor`。

---

## 九、OPT 批②逐条处置明细（it-opt-editor2 波，横切设施，2026-08-22）

- 输入：深查报告 B（Blackboard `runs/r2-20260822/results/it-survey-b`，OPT 33 锚点快核）＋登记文件《全局调查-优化建议-第二轮.md》批②条目（附篇 B 施工要点）。
- 施工基线：`1b717e9`（批①③ docs 收束后，锚点按当前 HEAD 逐处复测——O2-04/O2-17 落地后 NekoWalletManager/DailySignInManager 等行号有 +5~17 漂移，全部重新对齐）。
- 总结果：**批② 6 条全部实施（6 个独立 commit，每条 build 全绿后提交）；批④⑤⑥ 未触碰（标"待后续波/登记"）；依赖链口径兑现——O2-11 缓办不动（O2-03 仅收编立即任务队列）、O2-06 只做不依赖编辑域的一期。**

### [IT-O2-03] 抽公共 ServerTaskScheduler 收编三份同构队列【架构】 — 已实施
- 深查结论：三定义（MailHandler/LotteryHandler/DailySignInHandler 的 SERVER_TASKS+scheduleServerTask+消费循环 ≈75 行重复）与四消费方（MailActionPacket:154/LotteryRequestPacket:90/SignInRequestPacket:115/NekoEditPacket:130 借道 mail 的反向依赖）当前树全部命中；编辑任务失败打"执行邮件任务失败"误导文案确认。
- 处置：新建 `util/ServerTaskScheduler`（schedule + onServerTick END 消费 + INSTANCE 注册于 preInit）；NekoEditPacket 改指 util（trade→mail 反向依赖消除）；三份定义与消费循环删除（各 Handler 只留自身周期保存/跨日检查/延迟出货职责）；**偏差 1 处**：登记写的"ServerStoppingEvent 清队"在 1.7.10 FML 不投递给事件总线监听器（仓内 NekoWalletHandler 类注释自证），改由 `CommonProxy.serverStopping` 生命周期钩子调 `ServerTaskScheduler.clear()`，语义等价（停服清队防跨世界残留）；`LotteryHandler.scheduleDelayedTask` 抽奖动画专用墙钟语义按登记保留不并入。
- Commit：`b4e9c48`（refactor，+117/-106）
- 验证：build 全绿；全仓 grep `scheduleServerTask` 定义仅剩 ServerTaskScheduler，四消费方全部改指。

### [IT-O2-12] 抽 util.PlayerLookup 统一在线玩家表访问【一致性】 — 已实施
- 深查结论：18 处 playerEntityList 线性扫描当前树逐处命中（survey 后 NekoWalletManager :317→:306 等行号漂移已对齐）；四种形态（Tab 补全 4/UUID 集 2/全体遍历 6/单个查询 7 含 O(n) isPlayerOnline）与登记一致。
- 处置：新建 `util/PlayerLookup` 五 API（getOnlinePlayerByUuid/getOnlinePlayerByName/getOnlineNames/forEachOnlinePlayer/buildUuidSet，java.util.function.Consumer 为 Java 8 stdlib，jabel 安全）；18 处逐形态替换（GTITGiftCommand 9、MailManager 2、BlessingManager/LotteryNetworkManager/DailySignInManager/SignInNetworkManager/NekoWalletManager.buildOnlinePlayerCache/NekoNotificationScheduler/NekoTradeNetworkManager 各 1）；MailManager/DailySignInManager.getPlayerByUUID 保留公共签名改为委托（外部调用方 LotteryManager 等零改动）；NekoNotificationScheduler 大循环体整体改 lambda（内层 continue 语义保留）；isPlayerOnline O(n) 扫描改单次查询。
- Commit：`f384a04`（refactor，+188/-137）
- 验证：build 全绿；全仓 grep `playerEntityList` 仅剩 PlayerLookup 内部三处。

### [IT-O2-06] GTITGiftCommand 拆分·一期 PlayerResolver【架构】 — 已实施（一期）
- 深查结论：resolvePlayerUuid(:1413-1426) 与 usercache 手写 JSON 解析器簇（findUuidFromUserCache/findNameFromUserCache/parse×2/extract×3，:754-880）原位；mail 模块运行期反向依赖 command 类（MailManager:393 + import :20）确认。二期（五组子命令拆文件）依赖编辑域进度，按调度留后续（E1-E4 未做故 C1-C3 只做不依赖编辑域的一期——前提成立，未降级）。
- 处置：新建 `util/PlayerResolver`——resolvePlayerUuid（在线分支委托 PlayerLookup.getOnlinePlayerByName，大小写不敏感语义同 func_152612_a）+ usercache 解析簇全量移入 + 新增 getUserCacheFile() 统一 usercache 定位；GTITGiftCommand 删 158 行簇、4 处内部调用改指；MailManager 改指 PlayerResolver 并删 GTITGiftCommand import（反向依赖消除）。
- Commit：`b831c35`（refactor，+193/-158）
- 验证：build 全绿；mail/lottery/signin/trade 四域 grep `GTITGiftCommand` 归零。
- 执行偏差 1 处（已自纠）：簇删除时行区间多删 sendMailHelp 的 javadoc 开头导致其被注释吞没，编译暴露后按原文精确恢复，最终 diff 核对无净变化。

### [IT-O2-B01] 余额推送通道自立 WalletBalanceChannel【架构】 — 已实施
- 深查结论：NekoWalletManager 对 LotteryNetworkManager 的 import+三调用（isInitialized 门/团队冲刷/个人冲刷）当前树命中（survey 后 O2-04 已将 flushTeamBalance 改走门面，调用形态稳定）；LotteryClientData.balances 客户端缓存消费方 grep 枚举=LotteryGui:262/:985 两处（NekoVMGuiV2 不读余额）。
- 处置：新建 `trade.WalletNetworkManager`（通道 `gtit_wallet`，id=0）+`trade.WalletBalancePacket`（四要素范式照抄 LotteryNetworkManager，编码同构）+`trade.NekoClientBalances` 客户端静态缓存；NekoWalletManager 三调用点改指新通道；LotteryNetworkManager.sendBalanceToClient 保留为 @Deprecated 转发（一个版本后删），id=3 退役、LotteryBalancePacket 删除（通道回归纯抽奖）；LotteryClientData 的 balances/getBalance/updateBalances 维度迁出，LotterySyncPacket 处理器与 LotteryGui 两处改读写 NekoClientBalances；NekoWalletHandler 增 onPlayerLoggedIn 登录全量补推（防首包竞态，参照 LotterySyncPacket 做法），snapshotBalances private→包可见。
- Commit：`625b823`（refactor，+182/-76）
- 验证：build 全绿；trade 包 grep `LotteryNetworkManager` 归零；CommonProxy.init 增 WalletNetworkManager.init() 注册行。

### [IT-O2-B02] GTInterestingThing.LOG 去中心化【一致性】 — 已实施
- 深查结论：survey 记 379 点；批①③落地后当前树实测 414 处/54 文件（批①③ 新增 TeamDataProvider/O2-04 改写等增量）——按当前树全量处理。
- 处置：52 个非 main 包文件逐类声明 `private static final Logger LOG = LogManager.getLogger("gtit")`（与主类同 logger 名，日志过滤口径不变），机械替换调用点并删 logger 型 import；main 包入口自身（CommonProxy:85/ClientProxy:2 同包直引）按登记维持；infinitycell 2 文件因 MODID 引用保留 main import；MTENekoVendingMachineV2 的全限定调用一并归一。**微偏差 1 处**：NekoMusicConfig 原有自备 LOG 用名 "GTIT"（先例独立决策），其 1 处原 GTInterestingThing.LOG 调用现走该 logger——单条日志通道从 "gtit" 变 "GTIT"，其余 51 文件全部 "gtit" 不变。
- Commit：`9ac2554`（chore，54 files，+618/-402）
- 验证：build 全绿；`git grep "GTInterestingThing.LOG"` 仅剩 main 两入口；非 main 包 `import com.miaokatze.gtit.main` 归零。

### [IT-O2-B03] NekoCurrencyRegistrar 上提共享内核 currency 包【架构】 — 已实施
- 深查结论：survey 记 96 调用点/16 文件（限定调用口径）；当前树实测 26 文件含引用（含 import 行），纯消费域（mail/lottery/signin/machine.v2/client.gui）词汇性依赖确认。
- 处置：git mv 至新建 `com.miaokatze.gtit.currency` 包＋package 声明改写＋类注释补迁移说明；24 文件 import 与全限定引用机械改写；trade/trade.v2 同包裸引用 2 文件（NekoWallet/NekoPageRegistry）补 import；init 时机与静态字段（nekoCoinItem 等）语义零变化（纯搬家）。wiki `mods/gtit/trade` source_path 同步为仓外动作，移交主代理处置。
- Commit：`39b57f7`（refactor，25 files）
- 验证：build 全绿；全仓 grep `trade.NekoCurrencyRegistrar` 归零。

## 十、批②波阻塞与偏差记录

- 无构建阻断、无 revert。
- spotlessJavaCheck 失败 2 次（O2-12 换行风格、O2-B02 大批量机械替换后），均以管线内置 `gradlew spotlessApply` 修复后 build 全绿——与批①③同型，非逻辑问题、非新依赖。
- O2-06 一期簇删除的 sendMailHelp 吞没偏差见上节（编译期自纠，最终无净变化）。
- 语义等价性偏差 2 处均已在上节登记（O2-03 清队挂点改生命周期钩子；NekoMusicConfig 单点 logger 名）。

## 十一、批②波收尾状态

- 工作区 clean；`1b717e9..HEAD` 共 6 个实现 commit（refactor 5 / chore 1）+ 本汇总 docs commit。
- 每条目独立 `./gradlew build` 全绿；jabel 管线全程在位，无新依赖、未触碰 libs/ 与 VendingMachine、6000+ 行热点未做结构性拆分、无 git Tag。
- **批④（编辑域）待后续波**：O2-05 策略表（E1-E4，A03 施工图）。
- **批⑤（GUI/机器大件）待后续波**：O2-B04 + O2-08 G0-G6 + O2-07 M1/M2（含 O2-19）；O2-15 折入 G0；A01/A02 蓝图、A04 归 C1-C3。
- **批⑥（缓办触发制）待登记不动**：O2-10 / O2-11 / O2-20 / O2-B05 / O2-B06 / O2-B07。
- 报告已 post：Blackboard `runs/r2-20260822/results/it-opt-editor2`。


---

## 十二、OPT 批④逐条处置明细（it-opt-editor3 波，编辑域 O2-05，2026-08-22）

- 输入：深查报告 B（Blackboard `runs/r2-20260822/results/it-survey-b`，OPT 部分）＋登记文件《全局调查-优化建议-第二轮.md》O2-05 条目＋附篇 A O2-A03 施工图（E1-E4 分批表为施工图）。
- 施工基线：`f3b7229`（批② docs 收束后，工作区 clean 实测 git rev-parse）。
- 总结果：**O2-05 编辑域批 E1→E2→E3→E4 严格按 A03 蓝图顺序全部落地（4 个独立 commit，每步 `./gradlew build` 全绿后提交）；批⑤⑥ 未触碰。** 每步独立可 build；jabel（Java 8 语法面）全程在位。

### [IT-O2-05-E1] 策略表骨架 + 行为等价切换【架构】 — 已实施
- 处置：新建 `EditAction`（统一 4 参策略接口：player/targetId/targetIndex/jsonPayload）/ `EditActionRegistry`（15 ACTION 以方法引用（saveTrade 4 参精确匹配）+ lambda（签名差异显式落注册行）等价注册）/ `EditActionsCommon`（parseItemEntries + sendSuccess/sendError/sendInfo 自 handler 工具段逐字搬移，handler 侧 static import 维持原调用形式）；NekoEditPacket 15 case 箭头 switch 换 registry 分发、15 个 handleXxx 转发方法（:174-239 共 66 行）整体删除；白名单闸 0..14 连续区间判定收编为注册表存在性判定（A03 §3.3-4 深化点，非连续 ACTION 号不再隐性受限）。
- Commit：`1dc2cf9`（refactor，5 files，+220/-139）
- 验证：build 全绿；15 ACTION 注册逐一核对无重复无遗漏。

### [IT-O2-05-E2] TradeEditActions + PageEditActions 搬移【架构】 — 已实施
- 处置：交易域三入口（openTradeEditor/saveTrade/createTrade，含 saveTrade 双防御空物品校验与 v1.7.6 G3② 货币解绑链）逐字搬移至 `TradeEditActions`（328 行）；标签页域五件（createPage/savePage/deletePage + findPageEntry/applyPageEditJson，含默认页 1-3 删除拦截与交易迁往"其他"页口径）搬移至 `PageEditActions`（262 行）；注册表 6 ACTION 改指域类；handler 删对应段与失效 import（-534 行）。域后置动作不统一约束遵守（trade 域 sendSyncToAll、page 域 reload+sendSyncToAll 各自自带，未抽象统一钩子）。
- Commit：`103c8dd`（refactor，4 files，+600/-534）
- 验证：build 全绿；handler 内 trade/page 方法与相关 import 引用归零。

### [IT-O2-05-E3] SignInEditActions + OnlineTierEditActions 搬移【架构】 — 已实施
- 处置：签到域六件（openSignInEditor/saveSignInReward 四类 targetId 分派 + saveSignInMonthly/saveSignInTier/saveSignInDayOverride/parseReward，统一奖励模型 v1.7.8）逐字搬移至 `SignInEditActions`（262 行）；在线档位域两件（saveOnlineTier add/update/remove 三态 + formatDuration，v1.7.7 G5②）独立成 `OnlineTierEditActions`（129 行，A03 §3.3-1：online 自 signin 独立为第 6 域类）；注册表 3 ACTION 改指；handler -329 行。
- Commit：`c30aff7`（refactor，4 files，+395/-333）
- 验证：build 全绿；AnniversaryEntry 仍留在 handler（祝福域用，E4 迁）核对无误删。

### [IT-O2-05-E4] LotteryEditActions + BlessingEditActions 搬移 + 收口【架构】 — 已实施
- 处置：抽奖域九件（openLotteryEditor/saveLotteryEntry/saveLotteryPool/createLotteryPool/deleteLotteryPool + findLotteryPool/applyPoolEditJson/parsePoolCostItems/syncLegacyCurrencyFields，含 v1.7.0 目标 5 全服广播链与卡池 ID 字符集白名单）搬移至 `LotteryEditActions`（489 行）；祝福域四件（saveBlessing 三 targetId 分派 + applyBirthdayEditJson/applyFestivalEditJson/parseBlessingItems，落盘即生效无 reload）搬移至 `BlessingEditActions`（196 行）；注册表末 7 ACTION 改指——15/15 全部域化、零 NekoEditActionHandler 方法引用；NekoEditActionHandler 收口为校验壳 107 行（validateEditRequest + REQUIRED_PERMISSION_LEVEL/MAX_JSON_PAYLOAD_LENGTH/MAX_TARGET_ID_LENGTH 常量；蓝图估 ~140）。
- Commit：`bde7b8b`（refactor，4 files，+702/-645）
- 验证：build 全绿；**逐字搬移机器验证：33/33 个搬移方法体与 f3b7229 原文空白归一后逐字节一致**（Python 花括号配对抽取 + re.\s+ 归一比对）；全部 9 个新类包级私有 + 私有构造 + 自有 "gtit" logger（O2-B02 口径）。

## 十三、批④波阻塞与偏差记录

- 无构建阻断、无 revert。
- 执行偏差 1 处（工序重排，蓝图终态零变化）：E4 的"NekoEditPacket 收口删 15 转发方法"前置到 E1——switch 被 registry 替换后转发方法即死代码，且 E2+ 搬移域方法会破坏其编译引用，前置删除是唯一可逐步独立 build 的路径；E4 实际内容为两域搬移 + 壳收口。A03 §3.2 目标形态逐项对账一致（NekoEditPacket 241→162 行，蓝图估 ~185）。
- lambda 注册行换行风格由管线内置 `gradlew spotlessApply` 归一，非逻辑问题、非新依赖。
- 白名单闸改注册表判定属 A03 §3.3-4 明示深化点，非顺手重构（拒绝文案与日志逐字保留）。

## 十四、批④波收尾状态

- 工作区 clean；`f3b7229..HEAD` 共 4 个实现 commit（refactor 4）+ 本汇总 docs commit。
- 每步独立 `./gradlew build` 全绿（E1 复跑 1 次共 5 次）；jabel 管线全程在位，无新依赖、无 git Tag、未触碰 MTE 库存接口组与 GUI 拆页 3.4-b 镜像约束面。
- 行数账（对 A03 §3.2）：git diff `f3b7229..HEAD` trade/v2 共 +1883/-1617（净 +266）；新文件合计 1,857 行（骨架三件 197 + 六域类 1,660），NekoEditActionHandler 1,617→107，NekoEditPacket 242→162——蓝图预估净 -20~+50 的下沿之上（域类 javadoc/类骨架比蓝图估行略厚，方法体逐字不变）。
- 遗留（非缺陷，移交批⑤顺带）：NekoVMGuiV2 :3548/:3898/:4149/:4394 与 BlessingConfig :48 共 5 处 `{@code NekoEditActionHandler#xxx}` javadoc 纯文本提及未随迁改写（非解析链接、无编译影响）。
- **批⑤（GUI/机器大件）前置已就绪待后续波**：O2-B04 + O2-08 G0-G6 + O2-07 M1/M2（含 O2-19）；O2-15 折入 G0；A01/A02 蓝图、A04 归 C1-C3。
- **批⑥（缓办触发制）按触发条件登记不动**：O2-10 / O2-11 / O2-20 / O2-B05 / O2-B06 / O2-B07。
- 报告已 post：Blackboard `runs/r2-20260822/results/it-opt-editor3`。

## 十五、批⑤波（GUI/机器大件批）实施记录

- **O2-B04**（`3dfafd9`）：NekoVMGuiV2 迁出 common.machine.v2 至 gui.vm 页族根包（O2-08 第 0 步）；MeTransferEntry 三参构造过渡放宽 public（M1 顶层化前过渡）。
- **O2-08 G0**（`d2f2a31`）：GUI 拆页前置清理，O2-15 死代码项折入（forceSyncOutputSlots/outputSlotRefs）；蓝图所列 28 处 System.out.println 实测早前轮次已清零。
- **O2-08 G1-G4**（`1cd7040`/`43e6513`/`13c2c0b`/`baa8bff`）：EditOverlayController 基础设施→Trade/Page→SignIn/OnlineTier→Blessing/LotteryEntry/LotteryPool 编辑器家族全部出仓至 gui.vm.edit；每批 sync ID 快照回归（37 具名通道唯一+8 面板注册顺序一致）、build 全绿。
- **O2-08 G5**（`cf50e39`）：StatusBar 编解码/GuiMusicController/CoinOpsController 三域出仓（纯服务端域不涉 Widget 树），27 项归一化比对全过。
- **O2-08 G6**（`e7a4c1e`）：TradePage 与 IoColumnPanel 拆分+页面状态分域下沉（最高风险批），三 sub-tab 页壳参数化收敛（蓝图 ~150×3 收敛为单方法，偏差登记）；gui. 限定改写 22 项规则反转后 14 项比对全过。
- **O2-07 M1**（`4d62544`）：ME 传输队列 collaborator 抽取（MeTransferEntry 55 行 + NekoMeTransferQueue 271 行顶层化，MTE -252 行中大幅瘦身）；含上一波中断半成品收尾。
- **O2-07 M2 / O2-19**：按登记口径登记不实施（登记文件触发条件/范围未覆盖当前形态）。

## 十六、批⑥（缓办触发制）三态登记

O2-10 / O2-11 / O2-20 / O2-B05 / O2-B06 / O2-B07 共 6 条：逐条核对登记文件触发条件，均**登记"触发未满足/后续轮"**，本轮不实施（触发条件与理由见各条目登记原文）。

## 十七、版本收束与 token 机制说明

- 版本 commit：`89cc467`（gradle.properties RELEASE_VERSION 1.7.41→1.7.42）。
- **token 机制澄清（收束 it-close 波 BLOCKED 误判）**：本仓 `version` 由 gtnhsettingsconvention+palantir gitversion 从 git tag 派生（实测 `git describe` = v1.7.41-46-g89cc467 → 构建产物 v1.7.41-fix-20260822-r2.46+89cc46738d）；RELEASE_VERSION 供 GTNH 发布管线强制定版。mcmod.info `${modVersion}` 占位符**注入正常**（产物已展开为派生版本串），merge 回 master 并打 `v1.7.42` Tag 后产物自然显示 v1.7.42（第一轮 v1.7.41 同机制实证）。非缺陷，无需修复。

## 十八、终账

- IT 泳道 48 条（BUG 15 + OPT 33）全部三态落账：修复（带 commit sha，见上各节）／登记不实施（批⑥ 6 条+M2/O2-19 等）／降级（survey 阶段 0 条复活）。
- 新发现登记：NF-R2-01（6 组同型 C2S setter）登记不实施。
- 终验：`./gradlew build` 全绿；工作树 clean；未 push。
